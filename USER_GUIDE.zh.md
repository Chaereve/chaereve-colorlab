# 🎨 Chaereve ColorLab — 使用指南

一款在浏览器中查看、混合和导出 Photoshop（`.aco`）调色板的工具。
**无需安装、无需上传数据**——一切都在您的设备上运行。

---

## 目录
1. [快速上手](#1-快速上手)
2. [语言](#2-语言)
3. [“查看 ACO”标签页](#3-查看-aco标签页)
4. [“混合颜色”标签页](#4-混合颜色标签页)
5. [“随机颜色”标签页](#5-随机颜色标签页)
6. [设计工具——生成与分析](#6-设计工具生成与分析)
7. [批量处理](#7-批量处理)
8. [渐变 (.grd) 标签页](#8-渐变-grd-标签页)
9. [设置](#9-设置)
10. [导出图片与数据](#10-导出图片与数据)
11. [混合历史](#11-混合历史)
12. [安装为应用 (PWA)](#12-安装为应用-pwa)
13. [常见问题](#13-常见问题)

---

## 1. 快速上手

- 在任意浏览器（Chrome、Edge、Firefox、Safari…）中打开 `index.html`。
- 顶部有 **7 个标签页**：
  - **📁 查看 ACO** — 查看、编辑并导出 `.aco` 文件中的颜色。
  - **🎨 混合颜色** — 将多个颜色混合成一个新颜色。
  - **🎲 随机颜色** — 为成员生成随机颜色。
  - **🧩 生成** — 调色板生成器、渐变、图片取色、预设。
  - **🔍 分析** — 对比度、色盲、无障碍审计、统计。
  - **📦 批量** — 合并多个 `.aco` 文件。
  - **📐 渐变** — 打开并编辑 Photoshop 的 `.grd` 渐变文件。
- **⚙️ 设置**按钮（右上角，主题按钮旁）打开设置面板——主题、强调色、语言、字号、音效、动画、清除数据和关于。

---

## 2. 语言

- 打开 **⚙️ 设置**（右上角），在**语言**菜单中选择：越南语、英语、中文（简体）、한국어、日本語、Español。
- 整个界面即时切换，下次打开时仍会记住您的选择。

---

## 3. “查看 ACO”标签页

### 打开文件
- **拖放** `.aco` 文件到中央区域，**或**点击该区域选择文件。
- 支持 ACO **v1 与 v2**，可读取**颜色名称**和 RGB / HSB / CMYK / Lab / 灰度色彩空间。

### 查看颜色
- 每个颜色显示为卡片：**序号**（1、2、3…）、色块、名称、HEX、RGB 及其原始色彩空间。
- 序号按 **ACO 文件中的顺序**排列，便于对照。

### 复制颜色代码
- **点击色块**即可复制其代码（格式可选：HEX / RGB / HSL——在工具栏的格式菜单中选择）。
- **📋 全部复制**可将所有颜色复制为 HEX 列表、CSS 变量、SCSS、JSON 或 RGB。

### 选择多个颜色
- 点击卡片角落的 **✓** 来选中/取消。
- **☑ 选择** → 全选 / 取消全选 / 反选。

### 搜索与排序
- **🔍** 按名称、HEX 或 RGB 搜索。
- 按原始顺序、色相、明度、饱和度或名称排序。

### 高级搜索
在搜索框中输入特殊标记进行精确筛选：
- `hue:0-60` — 色相在该范围内的颜色（如红/黄）。
- `hue:>200` / `hue:<40` / `hue:120` — 大于、小于或精确色相。
- `sat:>50` — 饱和度大于 50；`light:40-80` — 明度在 40 到 80 之间。
- `similar:#FF0000` — 接近该颜色的颜色（RGB 距离 ≤ 80）。
- 普通关键词仍可用；可组合多个标记。

### 编辑调色板
- **✏️ 重命名**颜色、**🗑 删除**一个颜色，或 **➕ 添加**新颜色。
- 使用 **↩ 撤销 / ↪ 重做**来回退/前进（最多 60 步）。
- **右键**色块打开快捷菜单：复制、详情、重命名、将单个颜色导出为 `.aco` 或删除。
- **💾 保存 .aco** 将编辑写回新的 `.aco` 文件。

### 分享调色板
- 点击工具栏的 **🔗 分享**，复制形如 `…?palette=…` 的链接。
- 打开该链接即可加载相同的调色板——无需服务器或账户。

---

## 4. “混合颜色”标签页

此标签页可**独立使用**——无需先打开 ACO 文件。

### 添加要混合的颜色
两种方式：
1. **取色器** + **➕ 添加此颜色**——选择任意颜色。
2. **📁 打开 ACO 文件**——选择 `.aco`，会出现**选择网格**：
   - 默认选中所有颜色（✓）。
   - **点击色块**以排除**不想混合**的颜色。
   - **全选 / 取消 / 反选**按钮。
   - 点击 **➕ 添加 N 个颜色到混合**将所选颜色加入。

### 无需重新上传即可重新调整颜色
- 点击 **🔁 重新调整颜色**以**重新打开已加载 ACO 的选择网格**（无需重新上传）。
- 勾选/取消，然后点击“添加颜色到混合”——应用会**添加新颜色并移除您取消的颜色**。

### 颜色比例
- 比例按颜色数量**自动均分**（例如 4 个颜色 → 各 25%）。
- **⚖️ 均衡比例**可随时重置。
- **拖动滑块**提高/降低单个颜色——其余会自动重新计算至总和 100%。

### 混合结果
- 结果框显示混合后的颜色及 **HEX / RGB / HSL**，比例变化时即时更新。
- **📋 复制 HEX / RGB / HSL** 快速复制。
- **💾 保存到历史**保留混合后的颜色。

---

## 5. “随机颜色”标签页

生成随机颜色，每个颜色对应一个“成员”。

- **模式：** 🌈 完全随机，或 🎯 按色系（红 / 橙 / 黄 / 绿 / 青 / 蓝 / 紫 / 粉）。
- **颜色数量** = 成员数量。
- **不重复颜色**——勾选以避免重复。
- 点击 **🎲 随机生成**，然后**点击颜色卡片**复制（序号 = 成员）。
- **📋 全部复制**复制为 `成员 #1: #XXXXXX` 行。

---

## 6. 设计工具——生成与分析

设计工具分为两个标签页：**🧩 生成**和 **🔍 分析**。

### 🧩 生成标签页
| 工具 | 功能 |
|------|------|
| 🧩 **调色板生成器** | 从基色创建和谐调色板（互补、类似、三角、分裂、四角）。 |
| 🌈 **渐变生成器** | 构建线性 / 径向 / **圆锥** CSS 渐变并复制 CSS。 |
| 🖼 **图片 → 调色板 / 取色器** | 点击图片取单色、提取主色，并直接导出到 `.aco`。 |
| 📦 **预设与模板** | 一键使用内置调色板（Material、Tailwind、品牌色、粉彩、大地色）。 |

### 🔍 分析标签页
| 工具 | 功能 |
|------|------|
| 🔳 **对比度检查器** | 显示文字与背景间的 WCAG 对比度，含 AA/AAA 徽章。 |
| 👁 **色盲预览** | 以红色盲 / 绿色盲 / 蓝色盲 / 灰度视角查看调色板。 |
| ♿ **无障碍审计** | 调色板对白/黑文字达到 AA 的比例。 |
| 📊 **调色板统计** | 平均饱和度/明度及色相分布图。 |

此外，点击任意颜色卡片的 **ℹ️** 会打开**详情面板**，显示 HEX / RGB / HSL / HSV / CMYK / Lab、亮度与对比度（点击任意行可复制）。

---

## 7. 批量处理

在 **📦 批量**标签页：

- 点击**打开文件**，一次选择**多个 `.aco` 文件**（或拖放）。
- 列表显示每个文件及其颜色数；点击 ✕ 移除单个文件。
- **合并到查看器**将它们合并到“查看 ACO”标签页（可用“去重”复选框）。
- **导出合并的 .aco** 下载单个 `merged-palette.aco`。

---

## 8. 渐变 (.grd) 标签页

在 **📐 渐变**标签页中，您可以打开、编辑和导出 Photoshop 渐变文件（`.grd`，v3 与 v5）。

### 打开文件
- 点击 **📁 打开 .grd 文件**（或将 `.grd` 拖放到标签页）以加载其渐变。
- 每个渐变在列表中显示**实时预览**及 Solid/Noise 徽章；点击一个即可编辑。

### 编辑纯色渐变
- 在顶部字段**重命名**；**🗑** 删除渐变。
- **➕ 添加渐变**创建新的黑→白渐变。
- **色标：** 点击色块更改颜色，拖动**位置**（0–100%）与**中点**塑造过渡，**✕** 移除色标，**➕ 添加色标**在中间插入一个。
- **透明度标：** 同理——每个标有透明度与位置。
- **平滑度**控制整体插值。

### 编辑噪点渐变
- **种子 / 🎲 重新生成**、**粗糙度**、**色彩空间**、**添加透明度**、**限制颜色**，以及每通道的**最小 / 最大**值。
- 噪点渐变为**近似**重建（Photoshop 使用其内部算法）。

### 导出
- **📋 复制 CSS** → `linear-gradient(…)` 字符串。
- **📐 导出 SVG** → 独立矢量渐变。
- **🎨 提取颜色 → .aco** → 将色标保存为调色板。
- **📁 载入查看器** → 将颜色发送到“查看 ACO”标签页。
- **💾 保存 .grd** → 将所有渐变写回 `.grd` 文件。

> **注意：** 前景/背景标在打开时解析为具体颜色，保存时始终写为带 RGB 颜色的用户标。

---

## 9. 设置

**⚙️ 设置**标签页可个性化应用。每个选择都保存在您的设备上。

| 设置 | 功能 |
|------|------|
| **外观** | **浅色**、**深色**或**自动**（跟随系统）。右上角 🌙/☀️ 按钮仍可即时切换浅色/深色。 |
| **强调色** | 选择高亮色——重新着色 logo 文字、活动标签页、按钮和发光效果。 |
| **语言** | 从 6 种语言中选择：越南语、英语、中文（简体）、한국어、日本語、Español。 |
| **字号** | 小 / 中 / 大。 |
| **音效** | 在点击、复制和通知时播放轻柔音效（开/关）。 |
| **动画效果** | 打开或关闭动画。 |
| **清除已保存数据** | 删除混合历史、语言、主题和所有存储的偏好，然后重新加载。 |
| **关于** | 应用的简短描述。 |

---

## 10. 导出图片与数据

在 **查看 ACO** 标签页中，选择要导出的颜色，然后点击 **⬇ 导出图片与数据**。对话框可让您选择：

- **布局：** 水平条 / 垂直条 / 网格（含列数）。
- **单元格大小**和**间距**。
- **标签：** HEX / HEX + 名称 / 无。
- **背景：** 白色或透明。
- 下载前的**实时预览**。

导出格式：

| 格式 | 描述 |
|------|------|
| 🖼 PNG | 高分辨率调色板图片 |
| 📐 SVG | 矢量、可编辑 |
| 🎨 CSS | `--color-1: #…;` 变量 |
| { } JSON | 结构化颜色数据 |
| 📄 TXT | HEX 列表，每行一个 |

您还可以用 **💾 导出 .aco** 按钮将所选颜色导出回 **`.aco`** 文件。

---

## 11. 混合历史

- 位于 **混合颜色**标签页底部。
- 每当您**复制**或**保存**一个混合颜色，它都会自动加入历史。
- **点击历史色块**再次复制。
- **✕** 移除单个颜色；**🗑 清除历史**移除全部。
- 历史**保存在浏览器中**（重新打开页面时仍保留）。

---

## 12. 安装为应用 (PWA)

您可以在桌面和移动端安装该应用：

| 平台 | 方法 |
|------|------|
| **桌面**（Chrome/Edge） | 打开网址 → **📲**按钮或地址栏安装图标 → **安装** |
| **Android**（Chrome） | 打开网址 → **⋮**菜单 → **添加到主屏幕** |
| **iPhone/iPad**（Safari） | 打开网址 → **分享** → **添加到主屏幕** |

安装后：拥有独立图标，在**独立窗口**中打开（无地址栏），并可**离线**工作。

> **要求：** 应用必须通过 **HTTPS** 提供服务（GitHub Pages / Netlify / Vercel）。无法从本地 `file://` 安装。

---

## 13. 常见问题

**问：** 打开文件但没有颜色？
**答：** 检查是否为有效的 `.aco` 文件。部分软件导出的 ACO 结构不同。

**问：** 我的数据会被上传吗？
**答：** 不会。应用完全在浏览器中运行——不会通过网络发送任何内容。

**问：** 混合是如何工作的？
**答：** 颜色按比例混合（加权平均）——像混合颜料。拖动滑块让颜色“更重”。

**问：** 如何只混合 ACO 文件的一部分？
**答：** 点击 **📁 打开 ACO 文件**，取消勾选不想混合的颜色，然后点击**添加颜色到混合**。
