# 🎨 Chaereve ColorLab — ユーザーガイド

ブラウザでPhotoshop(`.aco`)カラーパレットを表示・混合・書き出しするツールです。
**インストール不要、データのアップロードなし** — すべて端末上で処理されます。

---

## 目次
1. [はじめに](#1-はじめに)
2. [言語](#2-言語)
3. [「ACOを表示」タブ](#3-acoを表示タブ)
4. [「色の混合」タブ](#4-色の混合タブ)
5. [「ランダムカラー」タブ](#5-ランダムカラータブ)
6. [デザインツール — 作成と分析](#6-デザインツール--作成と分析)
7. [バッチ処理](#7-バッチ処理)
8. [グラデーション (.grd) タブ](#8-グラデーション-grd-タブ)
9. [設定](#9-設定)
10. [画像とデータを書き出し](#10-画像とデータを書き出し)
11. [混合履歴](#11-混合履歴)
12. [アプリとしてインストール (PWA)](#12-アプリとしてインストール-pwa)
13. [よくある質問](#13-よくある質問)

---

## 1. はじめに

- 任意のブラウザ(Chrome、Edge、Firefox、Safari…)で`index.html`を開きます。
- 上部に**7つのタブ**があります:
  - **📁 ACOを表示** — `.aco`ファイルの色を表示・編集・書き出し。
  - **🎨 色の混合** — 複数の色を1つの新しい色に混ぜます。
  - **🎲 ランダムカラー** — メンバーごとにランダムカラーを生成。
  - **🧩 作成** — パレット生成、グラデーション、画像から抽出、プリセット。
  - **🔍 分析** — コントラスト、色覚、アクセシビリティ監査、統計。
  - **📦 バッチ** — 複数の`.aco`ファイルを統合。
  - **📐 グラデーション** — Photoshopの`.grd`グラデーションファイルを開く/編集。
- **⚙️ 設定**ボタン(右上、テーマボタンの隣)で設定パネルを開きます — テーマ、アクセントカラー、言語、フォントサイズ、サウンド、効果、データ消去、このアプリについて。

---

## 2. 言語

- **⚙️ 設定**(右上)を開き、**言語**メニューから選択: Tiếng Việt、English、中文 (简体)、한국어、日本語、Español。
- インターフェース全体が即座に切り替わり、次回も選択が記憶されます。

---

## 3. 「ACOを表示」タブ

### ファイルを開く
- 中央の領域に`.aco`ファイルを**ドラッグ＆ドロップ**するか、**クリック**してファイルを選択します。
- ACO **v1とv2**に対応し、**カラー名**とRGB / HSB / CMYK / Lab / グレースケール色空間を読み取ります。

### 色を表示
- 各色はカードで表示されます: **インデックス番号**(1, 2, 3…)、スウォッチ、名前、HEX、RGB、元の色空間。
- インデックスは**ACOファイル内の順序**に従うため、簡単に照合できます。

### カラーコードをコピー
- **スウォッチをクリック**するとコードをコピーします(形式選択: HEX / RGB / HSL — ツールバーの形式メニューで選択)。
- **📋 すべてコピー**は全色をHEXリスト、CSS変数、SCSS、JSON、RGBとしてコピーします。

### 複数の色を選択
- カード隅の**✓**をクリックして選択/解除します。
- **☑ 選択** → すべて選択 / 選択解除 / 反転。

### 検索と並べ替え
- **🔍** 名前、HEX、RGBで検索します。
- 元の順序、色相、明度、彩度、名前で並べ替え。

### 高度な検索
検索ボックスに特殊トークンを入力して正確に絞り込みます:
- `hue:0-60` — その範囲の色相の色(例: 赤/黄)。
- `hue:>200` / `hue:<40` / `hue:120` — より大きい、より小さい、または正確な色相。
- `sat:>50` — 彩度50超; `light:40-80` — 明度40〜80。
- `similar:#FF0000` — その色に近い色(RGB距離 ≤ 80)。
- 通常の単語も引き続き使用でき、複数のトークンを組み合わせられます。

### パレットを編集
- 色の**名前変更**(✏️)、**削除**(🗑)、**追加**(➕)。
- **↩ 元に戻す / ↪ やり直し**(最大60ステップ)を使用。
- スウォッチを**右クリック**してクイックメニュー: コピー、詳細、名前変更、単一色を`.aco`に書き出し、削除。
- **💾 .acoを保存**は編集内容を新しい`.aco`ファイルに書き出します。

### パレットを共有
- ツールバーの**🔗 共有**をクリックして`…?palette=…`形式のリンクをコピーします。
- リンクを開くと同じパレットが読み込まれます — サーバーやアカウントは不要です。

---

## 4. 「色の混合」タブ

このタブは**単体で**動作します — 先にACOファイルを開く必要はありません。

### 混合する色を追加
2つの方法:
1. **カラーピッカー** + **➕ この色を追加** — 任意の色を選択します。
2. **📁 ACOファイルを開く** — `.aco`を選択すると**選択グリッド**が表示されます:
   - デフォルトですべての色が選択済み(✓)。
   - **スウォッチをクリック**して**混ぜたくない色**を除外します。
   - **すべて選択 / 選択解除 / 反転**ボタン。
   - **➕ N色を混合に追加**をクリックして選択した色を入れます。

### 再アップロードせずに色を再調整
- **🔁 色を再調整**をクリックして**ロード済みACOの選択グリッドを再度開きます**(再アップロード不要)。
- チェックを付け外しして「混合に色を追加」をクリック — アプリが**新しい色を追加し、解除した色は削除**します。

### 色の比率
- 比率は色数に応じて**自動均等**に分けられます(例: 4色 → 各25%)。
- **⚖️ 比率を均等化**でいつでもリセットできます。
- **スライダーをドラッグ**して単一の色を上げ下げします — 残りが合計100%になるよう再計算されます。

### 混合結果
- 結果ボックスは混合色を**HEX / RGB / HSL**で表示し、比率変更時に即座に更新されます。
- **📋 HEX / RGB / HSLをコピー**で素早くコピー。
- **💾 履歴に保存**で混合色を保存します。

---

## 5. 「ランダムカラー」タブ

「メンバー」ごとにランダムカラーを生成します。

- **モード:** 🌈 完全ランダム、または 🎯 色系統別(赤 / オレンジ / 黄 / 緑 / シアン / 青 / 紫 / ピンク)。
- **色の数** = メンバー数。
- **重複色なし** — 繰り返しを避けるにはチェック。
- **🎲 ランダム生成**をクリックし、**カラーカードをクリック**してコピー(番号 = メンバー)。
- **📋 すべてコピー**は`メンバー #1: #XXXXXX`の形式でコピーします。

---

## 6. デザインツール — 作成と分析

デザインツールは**🧩 作成**と**🔍 分析**の2つのタブに分かれています。

### 🧩 作成タブ
| ツール | 機能 |
|--------|------|
| 🧩 **パレット生成** | 基本色から調和パレット(補色、類似、トライアド、スプリット、テトラード)を作成。 |
| 🌈 **グラデーション生成** | 線形 / 放射 / **円錐** CSSグラデーションを作成しCSSをコピー。 |
| 🖼 **画像 → パレット / スポイト** | 画像をクリックして色を取り、主要色を抽出して`.aco`に直接書き出し。 |
| 📦 **プリセット＆テンプレート** | 内蔵パレット(Material、Tailwind、ブランドカラー、パステル、アーストーン)をワンクリック。 |

### 🔍 分析タブ
| ツール | 機能 |
|--------|------|
| 🔳 **コントラストチェッカー** | テキストと背景のWCAGコントラスト比をAA/AAAバッジ付きで表示。 |
| 👁 **色覚異常プレビュー** | 第1色覚 / 第2色覚 / 第3色覚 / グレースケールの視点でパレットを表示。 |
| ♿ **アクセシビリティ監査** | 白/黒テキストに対してAAを満たすパレットの割合。 |
| 📊 **パレット統計** | 平均彩度/明度と色相分布チャート。 |

さらに、カラーカードの**ℹ️**をクリックすると**詳細パネル**が開き、HEX / RGB / HSL / HSV / CMYK / Lab、輝度、コントラストを表示します(行クリックでコピー)。

---

## 7. バッチ処理

**📦 バッチ**タブで:

- **ファイルを開く**をクリックし、**複数の`.aco`ファイル**を一度に選択(またはドラッグ＆ドロップ)。
- リストに各ファイルと色数が表示され、✕で個別のファイルを削除できます。
- **ビューアに統合**は「ACOを表示」タブにまとめます(「重複除去」チェックボックスで選択可)。
- **統合した.acoを書き出し**は単一の`merged-palette.aco`をダウンロードします。

---

## 8. グラデーション (.grd) タブ

**📐 グラデーション**タブでは、Photoshopグラデーションファイル(`.grd`、v3 & v5)を開き、編集し、書き出せます。

### ファイルを開く
- **📁 .grdファイルを開く**をクリック(または`.grd`をタブにドラッグ＆ドロップ)してグラデーションを読み込みます。
- 各グラデーションは**ライブプレビュー**とSolid/Noiseバッジ付きでリストに表示され、クリックで編集します。

### ソリッドグラデーションを編集
- 上部フィールドで**名前変更**; **🗑** でグラデーションを削除。
- **➕ グラデーションを追加**で新しい黒→白グラデーションを作成。
- **カラーストップ:** スウォッチをクリックして色を変更、**位置**(0–100%)と**中間点**をドラッグして遷移を調整、**✕** でストップを削除、**➕ カラーストップを追加**で中間に挿入。
- **不透明度ストップ:** 同様 — 各ストップの不透明度と位置。
- **滑らかさ**は全体の補間を制御します。

### ノイズグラデーションを編集
- **シード / 🎲 再生成**、**粗さ**、**色空間**、**透過を追加**、**色を制限**、チャネルごとの**最小 / 最大**値。
- ノイズグラデーションは**近似的に**再生成されます(Photoshopは独自の内部アルゴリズムを使用)。

### 書き出し
- **📋 CSSをコピー** → `linear-gradient(…)`文字列。
- **📐 SVGを書き出し** → 独立したベクターグラデーション。
- **🎨 色を抽出 → .aco** → カラーストップをパレットとして保存。
- **📁 ビューアに読み込み** → 色を「ACOを表示」タブへ送信。
- **💾 .grdを保存** → すべてのグラデーションを`.grd`ファイルに書き出します。

> **注意:** 前景/背景ストップは開く際に具体的な色に解決され、保存時は常にRGB色のユーザーストップとして記録されます。

---

## 9. 設定

**⚙️ 設定**タブでアプリをカスタマイズできます。すべての選択は端末に保存されます。

| 設定 | 機能 |
|------|------|
| **外観** | **ライト**、**ダーク**、または**自動**(システム追従)。右上の🌙/☀️ボタンは今でも即座に切り替えられます。 |
| **アクセントカラー** | ハイライトカラーを選択 — ロゴテキスト、アクティブタブ、ボタン、グロー効果を再配色します。 |
| **言語** | 6言語から選択: Tiếng Việt、English、中文 (简体)、한국어、日本語、Español。 |
| **フォントサイズ** | 小 / 中 / 大。 |
| **サウンド効果** | クリック、コピー、通知時に控えめな音を再生(オン/オフ)。 |
| **モーション効果** | アニメーションのオン/オフ。 |
| **保存データの消去** | 混合履歴、言語、テーマ、保存済みの設定をすべて削除し、再読み込みします。 |
| **このアプリについて** | アプリの簡単な説明。 |

---

## 10. 画像とデータを書き出し

**ACOを表示**タブで書き出す色を選択し、**⬇ 画像＆データを書き出し**をクリックします。ダイアログで選択できます:

- **レイアウト:** 横ストリップ / 縦ストリップ / グリッド(列数指定)。
- **セルサイズ**と**間隔**。
- **ラベル:** HEX / HEX + 名前 / なし。
- **背景:** 白または透明。
- ダウンロード前の**ライブプレビュー**。

書き出し形式:

| 形式 | 説明 |
|------|------|
| 🖼 PNG | 高解像度のパレット画像 |
| 📐 SVG | ベクター、編集可能 |
| 🎨 CSS | `--color-1: #…;`変数 |
| { } JSON | 構造化されたカラーデータ |
| 📄 TXT | HEXリスト、1行ごと |

**💾 .acoを書き出し**ボタンで、選択した色を**`.aco`**ファイルに書き出すこともできます。

---

## 11. 混合履歴

- **色の混合**タブの下部にあります。
- 混合色を**コピー**または**保存**するたびに、自動的に履歴に追加されます。
- **履歴のスウォッチをクリック**して再コピーします。
- **✕** で単一色を削除; **🗑 履歴を消去**で全削除。
- 履歴は**ブラウザに保存**されます(ページを再度開いても維持)。

---

## 12. アプリとしてインストール (PWA)

デスクトップとモバイルにインストールできます:

| プラットフォーム | 方法 |
|------------------|------|
| **デスクトップ** (Chrome/Edge) | URLを開く → **📲**ボタンまたはアドレスバーのインストールアイコン → **インストール** |
| **Android** (Chrome) | URLを開く → **⋮**メニュー → **ホーム画面に追加** |
| **iPhone/iPad** (Safari) | URLを開く → **共有** → **ホーム画面に追加** |

インストール後: 独自アイコン、**スタンドアロンウィンドウ**(アドレスバーなし)で開き、**オフライン**で動作します。

> **要件:** アプリは**HTTPS**(GitHub Pages / Netlify / Vercel)で提供される必要があります。ローカルの`file://`からはインストールできません。

---

## 13. よくある質問

**質問:** ファイルを開いても色が表示されません?
**回答:** 有効な`.aco`ファイルか確認してください。一部のソフトウェアは異なる構造でACOを書き出します。

**質問:** データはアップロードされますか?
**回答:** いいえ。アプリは完全にブラウザ内で動作し、ネットワークには何も送信されません。

**質問:** 混合はどのように機能しますか?
**回答:** 色が比率(加重平均)で混ざります — 絵の具を混ぜるように。スライダーをドラッグして色を「重く」します。

**質問:** ACOファイルの一部だけ混ぜるには?
**回答:** **📁 ACOファイルを開く**をクリック → 混ぜたくない色のチェックを外す → **色を混合に追加**をクリック。
