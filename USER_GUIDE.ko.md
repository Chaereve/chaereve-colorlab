# 🎨 Chaereve ColorLab — 사용자 가이드

브라우저에서 Photoshop(`.aco`) 색상 팔레트를 보고, 섞고, 내보내는 도구입니다.
**설치 불필요, 데이터 업로드 없음** — 모든 것이 기기에서 처리됩니다.

---

## 목차
1. [시작하기](#1-시작하기)
2. [언어](#2-언어)
3. ["ACO 보기" 탭](#3-aco-보기-탭)
4. ["색상 혼합" 탭](#4-색상-혼합-탭)
5. ["랜덤 색상" 탭](#5-랜덤-색상-탭)
6. [디자인 도구 — 만들기 & 분석](#6-디자인-도구--만들기--분석)
7. [일괄 처리](#7-일괄-처리)
8. [그라디언트 (.grd) 탭](#8-그라디언트-grd-탭)
9. [설정](#9-설정)
10. [이미지 & 데이터 내보내기](#10-이미지--데이터-내보내기)
11. [혼합 기록](#11-혼합-기록)
12. [앱으로 설치 (PWA)](#12-앱으로-설치-pwa)
13. [FAQ](#13-faq)

---

## 1. 시작하기

- 아무 브라우저(Chrome, Edge, Firefox, Safari…)에서 `index.html`을 엽니다.
- 상단에 **7개 탭**이 있습니다:
  - **📁 ACO 보기** — `.aco` 파일의 색상을 보고, 편집하고, 내보냅니다.
  - **🎨 색상 혼합** — 여러 색을 하나의 새 색으로 섞습니다.
  - **🎲 랜덤 색상** — 멤버별 랜덤 색상을 생성합니다.
  - **🧩 만들기** — 팔레트 생성기, 그라디언트, 이미지에서 추출, 프리셋.
  - **🔍 분석** — 대비, 색맹, 접근성 감사, 통계.
  - **📦 일괄** — 여러 `.aco` 파일 병합.
  - **📐 그라디언트** — Photoshop `.grd` 그라디언트 파일 열기/편집.
- **⚙️ 설정** 버튼(우측 상단, 테마 버튼 옆)이 설정 패널을 엽니다 — 테마, 강조 색상, 언어, 글자 크기, 사운드, 효과, 데이터 지우기, 정보.

---

## 2. 언어

- **⚙️ 설정**(우측 상단)을 열고 **언어** 메뉴에서 선택합니다: Tiếng Việt, English, 中文 (简体), 한국어, 日本語, Español.
- 전체 인터페이스가 즉시 전환되고 다음에 열 때도 선택이 유지됩니다.

---

## 3. "ACO 보기" 탭

### 파일 열기
- `.aco` 파일을 중앙 영역에 **드래그 앤 드롭**하거나, **클릭**하여 파일을 선택합니다.
- ACO **v1 및 v2**를 지원하며 **색상 이름**과 RGB / HSB / CMYK / Lab / Grayscale 색공간을 읽습니다.

### 색상 보기
- 각 색상은 카드로 표시됩니다: **색인 번호**(1, 2, 3…), 색상 칩, 이름, HEX, RGB 및 원래 색공간.
- 색인은 **ACO 파일의 순서**를 따르므로 쉽게 대조할 수 있습니다.

### 색상 코드 복사
- **색상 칩을 클릭**하면 코드가 복사됩니다(형식 선택: HEX / RGB / HSL — 도구 모음의 형식 메뉴에서 선택).
- **📋 전체 복사**는 모든 색을 HEX 목록, CSS 변수, SCSS, JSON 또는 RGB로 복사합니다.

### 여러 색 선택
- 카드 모서리의 **✓**를 클릭하여 선택/해제합니다.
- **☑ 선택** → 전체 선택 / 선택 해제 / 반전.

### 검색 & 정렬
- **🔍** 이름, HEX 또는 RGB로 검색합니다.
- 원래 순서, 색상, 밝기, 채도 또는 이름으로 정렬합니다.

### 고급 검색
검색 상자에 특수 토큰을 입력해 정밀하게 필터링합니다:
- `hue:0-60` — 해당 범위의 색상(예: 빨강/노랑).
- `hue:>200` / `hue:<40` / `hue:120` — 초과, 미만 또는 정확한 색상.
- `sat:>50` — 채도 50 초과; `light:40-80` — 밝기 40~80.
- `similar:#FF0000` — 해당 색과 가까운 색(RGB 거리 ≤ 80).
- 일반 단어도 여전히 사용 가능하며 여러 토큰을 조합할 수 있습니다.

### 팔레트 편집
- 색상 **이름 바꾸기**(✏️), **삭제**(🗑) 또는 **추가**(➕).
- **↩ 실행 취소 / ↪ 다시 실행**(최대 60단계) 사용.
- 색상 칩을 **우클릭**하여 빠른 메뉴: 복사, 상세, 이름 바꾸기, 단일 색 `.aco` 내보내기, 삭제.
- **💾 .aco 저장**은 편집 내용을 새 `.aco` 파일에 기록합니다.

### 팔레트 공유
- 도구 모음의 **🔗 공유**를 클릭해 `…?palette=…` 형식의 링크를 복사합니다.
- 링크를 열면 동일한 팔레트가 로드됩니다 — 서버나 계정이 필요 없습니다.

---

## 4. "색상 혼합" 탭

이 탭은 **독립적으로** 작동합니다 — ACO 파일을 먼저 열 필요가 없습니다.

### 혼합할 색 추가
두 가지 방법:
1. **색상 선택기** + **➕ 이 색 추가** — 아무 색이나 선택합니다.
2. **📁 ACO 파일 열기** — `.aco`를 선택하면 **선택 그리드**가 나타납니다:
   - 기본적으로 모든 색이 선택됨(✓).
   - **색상 칩을 클릭**하여 **섞고 싶지 않은 색**을 제외합니다.
   - **전체 선택 / 선택 해제 / 반전** 버튼.
   - **➕ N개 색을 혼합에 추가**를 클릭해 선택한 색을 넣습니다.

### 재업로드 없이 색 재조정
- **🔁 색 재조정**을 클릭해 **로드된 ACO의 선택 그리드를 다시 엽니다**(재업로드 불필요).
- 선택/해제 후 "혼합에 색 추가" 클릭 — 앱이 **새 색을 추가하고 해제한 색은 제거**합니다.

### 색상 비율
- 비율은 색상 수에 따라 **자동 균등 분배**됩니다(예: 4색 → 각 25%).
- **⚖️ 비율 균등화**로 언제든 재설정합니다.
- **슬라이더를 드래그**해 단일 색을 올리거나 내립니다 — 나머지가 합계 100%로 재계산됩니다.

### 혼합 결과
- 결과 상자는 혼합된 색을 **HEX / RGB / HSL**로 표시하며 비율 변경 시 즉시 업데이트됩니다.
- **📋 HEX / RGB / HSL 복사**로 빠르게 복사합니다.
- **💾 기록에 저장**으로 혼합 색을 보관합니다.

---

## 5. "랜덤 색상" 탭

"멤버"별 랜덤 색상을 생성합니다.

- **모드:** 🌈 완전 랜덤, 또는 🎯 색상 계열별(빨강 / 주황 / 노랑 / 초록 / 시안 / 파랑 / 보라 / 분홍).
- **색상 수** = 멤버 수.
- **중복 색 없음** — 반복을 피하려면 체크.
- **🎲 랜덤 생성** 클릭 후 **색상 카드를 클릭**해 복사(번호 = 멤버).
- **📋 전체 복사**는 `멤버 #1: #XXXXXX` 줄로 복사합니다.

---

## 6. 디자인 도구 — 만들기 & 분석

디자인 도구는 **🧩 만들기**와 **🔍 분석** 두 탭으로 나뉩니다.

### 🧩 만들기 탭
| 도구 | 기능 |
|------|------|
| 🧩 **팔레트 생성기** | 기본 색에서 조화 팔레트(보색, 유사, 삼각, 분할, 사각)를 만듭니다. |
| 🌈 **그라디언트 생성기** | 선형 / 방사형 / **원뿔** CSS 그라디언트를 만들고 CSS를 복사합니다. |
| 🖼 **이미지 → 팔레트 / 스포이드** | 이미지를 클릭해 색을 고르고, 주 색을 추출해 `.aco`로 바로 내보냅니다. |
| 📦 **프리셋 & 템플릿** | 내장 팔레트(Material, Tailwind, Brand Colors, Pastel, Earth Tones)를 원클릭 사용. |

### 🔍 분석 탭
| 도구 | 기능 |
|------|------|
| 🔳 **대비 검사기** | 텍스트와 배경의 WCAG 대비율을 AA/AAA 배지와 함께 표시. |
| 👁 **색맹 미리보기** | 적색맹 / 녹색맹 / 청색맹 / 회색조 시점으로 팔레트를 표시. |
| ♿ **접근성 감사** | 흰색/검정 텍스트에 대해 AA를 통과하는 팔레트 비율. |
| 📊 **팔레트 통계** | 평균 채도/밝기와 색상 분포 차트. |

또한 색상 카드의 **ℹ️**를 클릭하면 **상세 패널**이 열려 HEX / RGB / HSL / HSV / CMYK / Lab, 휘도, 대비를 표시합니다(행 클릭 시 복사).

---

## 7. 일괄 처리

**📦 일괄** 탭에서:

- **파일 열기** 클릭 후 **여러 `.aco` 파일**을 한 번에 선택(또는 드래그 앤 드롭).
- 목록에 각 파일과 색상 수가 표시되며 ✕로 개별 파일 제거.
- **뷰어로 병합**은 "ACO 보기" 탭으로 합칩니다(선택적 "중복 제거" 체크박스).
- **병합된 .aco 내보내기**는 단일 `merged-palette.aco`를 다운로드합니다.

---

## 8. 그라디언트 (.grd) 탭

**📐 그라디언트** 탭에서 Photoshop 그라디언트 파일(`.grd`, v3 & v5)을 열고, 편집하고, 내보낼 수 있습니다.

### 파일 열기
- **📁 .grd 파일 열기** 클릭(또는 `.grd`를 탭에 드래그 앤 드롭)하여 그라디언트를 로드합니다.
- 각 그라디언트가 **실시간 미리보기**와 Solid/Noise 배지로 목록에 표시되며 클릭하면 편집합니다.

### 솔리드 그라디언트 편집
- 상단 필드에서 **이름 바꾸기**; **🗑** 그라디언트 삭제.
- **➕ 그라디언트 추가**로 새 검정→흰색 그라디언트 생성.
- **색상 정지점:** 색상 칩을 클릭해 색 변경, **위치**(0–100%)와 **중간점**을 드래그해 전환 조정, **✕** 정지점 제거, **➕ 색상 정지점 추가**로 중간에 삽입.
- **투명도 정지점:** 동일 — 각 정지점의 불투명도와 위치.
- **매끄러움**은 전체 보간을 제어합니다.

### 노이즈 그라디언트 편집
- **시드 / 🎲 재생성**, **거칠기**, **색공간**, **투명도 추가**, **색상 제한**, 채널별 **최소 / 최대** 값.
- 노이즈 그라디언트는 **근사적으로** 재생성됩니다(Photoshop은 자체 내부 알고리즘 사용).

### 내보내기
- **📋 CSS 복사** → `linear-gradient(…)` 문자열.
- **📐 SVG 내보내기** → 독립 벡터 그라디언트.
- **🎨 색 추출 → .aco** → 색상 정지점을 팔레트로 저장.
- **📁 뷰어로 불러오기** → 색을 "ACO 보기" 탭으로 전송.
- **💾 .grd 저장** → 모든 그라디언트를 `.grd` 파일에 기록.

> **참고:** 전경/배경 정지점은 열 때 구체적인 색으로 해석되며, 저장 시 항상 RGB 색을 가진 사용자 정지점으로 기록됩니다.

---

## 9. 설정

**⚙️ 설정** 탭에서 앱을 개인화할 수 있습니다. 모든 선택은 기기에 저장됩니다.

| 설정 | 기능 |
|------|------|
| **화면** | **라이트**, **다크** 또는 **자동**(시스템 따름). 우측 상단 🌙/☀️ 버튼은 여전히 즉시 전환됩니다. |
| **강조 색상** | 하이라이트 색 선택 — 로고 텍스트, 활성 탭, 버튼, 글로우 효과를 다시 칠합니다. |
| **언어** | 6개 언어 중 선택: Tiếng Việt, English, 中文 (简体), 한국어, 日本語, Español. |
| **글자 크기** | 작게 / 중간 / 크게. |
| **사운드 효과** | 클릭, 복사, 알림 시 은은한 소리를 재생(켜기/끄기). |
| **모션 효과** | 애니메이션을 켜거나 끕니다. |
| **저장된 데이터 지우기** | 혼합 기록, 언어, 테마, 모든 저장된 선호를 제거하고 다시 로드합니다. |
| **정보** | 앱에 대한 짧은 설명. |

---

## 10. 이미지 & 데이터 내보내기

**ACO 보기** 탭에서 내보낼 색을 선택한 후 **⬇ 이미지 & 데이터 내보내기**를 클릭합니다. 대화 상자에서 선택할 수 있습니다:

- **레이아웃:** 가로 스트립 / 세로 스트립 / 그리드(열 수 포함).
- **셀 크기**와 **간격**.
- **라벨:** HEX / HEX + 이름 / 없음.
- **배경:** 흰색 또는 투명.
- 다운로드 전 **실시간 미리보기**.

내보내기 형식:

| 형식 | 설명 |
|------|------|
| 🖼 PNG | 고해상도 팔레트 이미지 |
| 📐 SVG | 벡터, 편집 가능 |
| 🎨 CSS | `--color-1: #…;` 변수 |
| { } JSON | 구조화된 색상 데이터 |
| 📄 TXT | HEX 목록, 줄당 하나 |

**💾 .aco 내보내기** 버튼으로 선택한 색을 **`.aco`** 파일로 다시 내보낼 수도 있습니다.

---

## 11. 혼합 기록

- **색상 혼합** 탭 하단에 있습니다.
- 혼합 색을 **복사**하거나 **저장**할 때마다 자동으로 기록에 추가됩니다.
- **기록 색상 칩 클릭**으로 다시 복사합니다.
- **✕** 단일 색 제거; **🗑 기록 지우기** 전체 제거.
- 기록은 **브라우저에 저장**됩니다(페이지를 다시 열어도 유지).

---

## 12. 앱으로 설치 (PWA)

데스크톱과 모바일에 설치할 수 있습니다:

| 플랫폼 | 방법 |
|--------|------|
| **데스크톱** (Chrome/Edge) | URL 열기 → **📲** 버튼 또는 주소창 설치 아이콘 → **설치** |
| **Android** (Chrome) | URL 열기 → **⋮** 메뉴 → **홈 화면에 추가** |
| **iPhone/iPad** (Safari) | URL 열기 → **공유** → **홈 화면에 추가** |

설치 후: 자체 아이콘, **독립 창**(주소창 없음)에서 열리고 **오프라인**으로 작동합니다.

> **요구 사항:** 앱은 **HTTPS**(GitHub Pages / Netlify / Vercel)로 제공되어야 합니다. 로컬 `file://`에서는 설치할 수 없습니다.

---

## 13. FAQ

**질문:** 파일을 열었는데 색상이 없어요?
**답변:** 유효한 `.aco` 파일인지 확인하세요. 일부 소프트웨어는 다른 구조로 ACO를 내보냅니다.

**질문:** 내 데이터가 업로드되나요?
**답변:** 아니요. 앱은 완전히 브라우저에서 실행되며 네트워크로 아무것도 전송되지 않습니다.

**질문:** 혼합은 어떻게 작동하나요?
**답변:** 색이 비율(가중 평균)로 섞입니다 — 물감을 섞는 것처럼요. 슬라이더를 드래그해 색을 더 "무겁게" 만듭니다.

**질문:** ACO 파일의 일부만 섞으려면?
**답변:** **📁 ACO 파일 열기** 클릭 → 원치 않는 색 체크 해제 → **혼합에 색 추가** 클릭.
